import { boolean, index, pgTable, timestamp, uuid, varchar, uniqueIndex } from 'drizzle-orm/pg-core';
import { invitationPurpose, userRole, userStatus } from './enums';
export const branches = pgTable('branches',{id:uuid('id').defaultRandom().primaryKey(),name:varchar('name',{length:120}).notNull(),address:varchar('address',{length:255}).notNull(),isActive:boolean('is_active').notNull().default(true),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull(),updatedAt:timestamp('updated_at',{withTimezone:true}).defaultNow().notNull()});
export const users = pgTable('users',{id:uuid('id').defaultRandom().primaryKey(),role:userRole('role').notNull(),phone:varchar('phone',{length:30}).notNull(),passwordHash:varchar('password_hash',{length:255}),status:userStatus('status').notNull(),defaultBranchId:uuid('default_branch_id').references(()=>branches.id,{onDelete:'restrict'}),lastLoginAt:timestamp('last_login_at',{withTimezone:true}),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull(),updatedAt:timestamp('updated_at',{withTimezone:true}).defaultNow().notNull()},t=>({phone:uniqueIndex('users_phone_uq').on(t.phone)}));
export const invitations = pgTable('invitations',{id:uuid('id').defaultRandom().primaryKey(),userId:uuid('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),purpose:invitationPurpose('purpose').notNull(),tokenHash:varchar('token_hash',{length:255}).notNull(),expiresAt:timestamp('expires_at',{withTimezone:true}).notNull(),usedAt:timestamp('used_at',{withTimezone:true}),invalidatedAt:timestamp('invalidated_at',{withTimezone:true}),createdBy:uuid('created_by').references(()=>users.id,{onDelete:'restrict'}),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull()},t=>({token:uniqueIndex('invitations_token_hash_uq').on(t.tokenHash), lookup:index('invitations_user_purpose_created_idx').on(t.userId,t.purpose,t.createdAt.desc())}));
export const sessions = pgTable('sessions',{id:uuid('id').defaultRandom().primaryKey(),userId:uuid('user_id').notNull().references(()=>users.id,{onDelete:'cascade'}),tokenHash:varchar('token_hash',{length:255}).notNull(),expiresAt:timestamp('expires_at',{withTimezone:true}).notNull(),createdAt:timestamp('created_at',{withTimezone:true}).defaultNow().notNull()},t=>({token:uniqueIndex('sessions_token_hash_uq').on(t.tokenHash),expiry:index('sessions_user_expires_idx').on(t.userId,t.expiresAt)}));

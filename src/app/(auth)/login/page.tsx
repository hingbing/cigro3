'use client'; import { useActionState } from 'react'; import { loginAction,type LoginActionState } from './actions'; const initialState:LoginActionState={}; export default function LoginPage(){const [state,formAction,pending]=useActionState(loginAction,initialState);return <main className="shell"><section className="card"><p className="badge">라운지핏</p><h1>라운지핏 로그인</h1><p>원하는 수업을 간편하게 예약해 보세요.</p><form action={formAction}><label htmlFor="phone">전화번호</label><input id="phone" name="phone" inputMode="tel" autoComplete="tel" required/><label htmlFor="password">비밀번호</label><input id="password" name="password" type="password" autoComplete="current-password" required/>{state.error&&<p className="error" aria-live="polite">{state.error}</p>}<button type="submit" disabled={pending}>로그인</button></form><section className="item"><h2>데모 계정</h2><p>관리자 · 010-9999-9999 / admin-password</p><p>회원 · 010-1234-5678 / demo-password</p></section></section></main>}
